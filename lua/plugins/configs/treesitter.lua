local options = {
  ensure_installed = { "lua", "vim", "html", "javascript", "typescript", "json", "tsx" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
